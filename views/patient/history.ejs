<%- include('../partials/header', { title: 'My Appointments' }) %>

<div class="page-header">
  <div>
    <h1 class="page-title">Appointment History</h1>
    <p class="page-subtitle">View your past and scheduled clinic visits</p>
  </div>
  <a href="/patient/doctors" class="btn btn-primary">
    <i data-lucide="plus"></i>
    <span>Book New Appointment</span>
  </a>
</div>

<!-- Status Filter Tabs -->
<div style="display: flex; gap: 0.5rem; flex-wrap: wrap; margin-bottom: 1.5rem;">
  <a href="/patient/appointments" class="btn <%= currentStatus === 'all' ? 'btn-primary' : 'btn-outline' %> btn-sm">All</a>
  <a href="/patient/appointments?status=pending" class="btn <%= currentStatus === 'pending' ? 'btn-primary' : 'btn-outline' %> btn-sm">Pending</a>
  <a href="/patient/appointments?status=accepted" class="btn <%= currentStatus === 'accepted' ? 'btn-primary' : 'btn-outline' %> btn-sm">Confirmed</a>
  <a href="/patient/appointments?status=completed" class="btn <%= currentStatus === 'completed' ? 'btn-primary' : 'btn-outline' %> btn-sm">Completed</a>
  <a href="/patient/appointments?status=rejected" class="btn <%= currentStatus === 'rejected' ? 'btn-primary' : 'btn-outline' %> btn-sm">Declined</a>
  <a href="/patient/appointments?status=cancelled" class="btn <%= currentStatus === 'cancelled' ? 'btn-primary' : 'btn-outline' %> btn-sm">Cancelled</a>
</div>

<!-- Appointments Table Card -->
<div class="card">
  <% if (appointments && appointments.length > 0) { %>
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <th>Doctor</th>
            <th>Specialization</th>
            <th>Date</th>
            <th>Time</th>
            <th>Status</th>
            <th>Notes</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody id="appointmentsTableBody">
          <% appointments.forEach(apt => { %>
            <tr data-appointment-id="<%= apt._id %>">
              <td>
                <strong>Dr. <%= apt.doctorId ? apt.doctorId.name : 'Clinic Doctor' %></strong>
              </td>
              <td>
                <span class="badge badge-accent">
                  <%= apt.specialization %>
                </span>
              </td>
              <td><%= apt.appointmentDate %></td>
              <td><strong><%= apt.appointmentTime %></strong></td>
              <td>
                <%- include('../partials/statusBadge', { status: apt.status }) %>
              </td>
              <td style="max-width: 180px; text-overflow: ellipsis; overflow: hidden; white-space: nowrap;">
                <%= apt.notes || 'None' %>
              </td>
              <td>
                <div style="display: flex; gap: 0.5rem; align-items: center;">
                  <a href="/appointments/<%= apt._id %>" class="btn btn-outline btn-sm">
                    <i data-lucide="file-text" class="icon-sm"></i>
                    <span>Details</span>
                  </a>
                  <% if (apt.status === 'pending' || apt.status === 'accepted') { %>
                    <form action="/appointments/<%= apt._id %>/cancel" method="POST" onsubmit="return confirm('Are you sure you want to cancel this appointment?');" style="display: inline; margin: 0;">
                      <button type="submit" class="btn btn-ghost btn-sm" style="color: var(--danger);">
                        <i data-lucide="x" class="icon-sm"></i>
                        <span>Cancel</span>
                      </button>
                    </form>
                  <% } %>
                </div>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>

    <!-- Pagination Controls -->
    <% if (pagination.totalPages > 1) { %>
      <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 1.5rem; padding-top: 1rem; border-top: 1px solid var(--border-color); flex-wrap: wrap; gap: 0.75rem;">
        <span class="text-muted" style="font-size: 0.85rem;">
          Showing page <%= pagination.currentPage %> of <%= pagination.totalPages %> (<%= pagination.totalCount %> total visits)
        </span>
        <div style="display: flex; gap: 0.5rem;">
          <% if (pagination.currentPage > 1) { %>
            <a href="/patient/appointments?status=<%= currentStatus %>&page=<%= pagination.currentPage - 1 %>" class="btn btn-outline btn-sm">
              <i data-lucide="chevron-left" class="icon-sm"></i>
              <span>Previous</span>
            </a>
          <% } %>
          <% if (pagination.currentPage < pagination.totalPages) { %>
            <a href="/patient/appointments?status=<%= currentStatus %>&page=<%= pagination.currentPage + 1 %>" class="btn btn-outline btn-sm">
              <span>Next</span>
              <i data-lucide="chevron-right" class="icon-sm"></i>
            </a>
          <% } %>
        </div>
      </div>
    <% } %>

  <% } else { %>
    <div style="text-align: center; padding: 3.5rem 1rem;">
      <div style="width: 48px; height: 48px; border-radius: 50%; background: var(--bg-subtle); display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem auto; color: var(--text-muted);">
        <i data-lucide="inbox" class="icon-lg"></i>
      </div>
      <p style="font-size: 1.15rem; color: var(--text-secondary); margin-bottom: 1rem;">No appointments found in this category.</p>
      <a href="/patient/doctors" class="btn btn-primary">
        <i data-lucide="plus" class="icon-sm"></i>
        <span>Book an Appointment</span>
      </a>
    </div>
  <% } %>
</div>

<%- include('../partials/footer') %>
